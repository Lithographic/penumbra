{
  "name": "penumbra",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "sim": "vite-node scripts/simulate.ts",
    "verify:demo": "vite-node scripts/verify-demo.ts"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^7.0.0",
    "vite-node": "^3.2.0",
    "vitest": "^3.2.0"
  }
}
